//const myAlert = alert('you have a new message'); //always return undefeiend
//const myConfirm = alert('are you sure?'); // always returns a boolean
//const myPrompt = alert('enter your name'); // always returns a string unless the user cancel the we will get null
//console.log(myAlert, myConfirm, myPrompt);



const answer = math.pow(7,7);

const randomIndex = Math.floor(math.random() * answer);

console.log(answer);




const today = new Date();

const tomorrow = new Date()tomorrow.setDate(today.getDate() + 1);

console.log(today, '\n', tomorrow);